node_modules/

# secrets & local config
.env

# runtime store: sqlite db, pid file, session transcripts, lark-cli config
# (contains the Feishu APP SECRET), and tmux scratch — NEVER commit any of it.
store/

# runtime workspace artifacts (the workspace/ dir itself is tracked, but not its runtime output)
workspace/uploads/
workspace/logs/

*.log
.DS_Store
